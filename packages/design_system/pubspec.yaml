name: design_system
description: Sistema de diseño para la demo bancaria.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.13.3
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
