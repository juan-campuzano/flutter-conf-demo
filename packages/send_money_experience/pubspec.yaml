name: send_money_experience
description: Experiencia de envío de dinero, desacoplable de la app principal.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.13.3
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
