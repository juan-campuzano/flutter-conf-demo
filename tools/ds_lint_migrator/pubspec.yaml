name: ds_lint_migrator
description: Reglas de lint y quick fixes para migrar entre versiones del design system.
version: 0.2.0
publish_to: 'none'

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.1
  path: ^1.9.0

dev_dependencies:
  custom_lint: ^0.8.1
  lints: ^6.0.0
